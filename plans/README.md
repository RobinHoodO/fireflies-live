# Implementation Plans

Generated by the `improve` skill on 2026-07-14 against commit `43c51bd`.
Advisor pass reviewed the whole system (v2 app + `server/bridge.mjs` + docs)
against its intention — the "during-meeting" layer of the sales OS. Four parallel
read-only audits (correctness, security, perf/debt/deps, tests/DX/docs) →
findings vetted against the code → the plans below.

Each executor: read the plan fully before starting, honor its STOP conditions,
run its verification gates, and update your row when done. Every plan is
self-contained (an executor with zero prior context can run it) and forbids
touching out-of-scope files.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (v2 typecheck + `node --test` + `npm run verify`) | P1 | S | — | DONE |
| 002 | Freeze bridge security guards as tests | P1 | M | 001 | DONE |
| 004 | Gate transcript-derived commands before `/pi` (+ `/pi` denylist) | P1 | M | 002 | DONE |
| 005 | Stop `/context` leaking the wrong client's docs | P1 | M | 002 | DONE |
| 003 | Test the pure LLM-output validators + md escaper | P2 | M | 001 | DONE |
| 006 | Four correctness fixes (env regex, pulse race, clean-boot agenda, map leak) | P2 | S | 001 | DONE |
| 008 | Retire legacy v1 app + drop 5 deps | P2 | M | 001 | DONE |
| 009 | Root CLAUDE.md for agent onboarding | P2 | S | 001, 008 | DONE |
| 010 | Stop re-rendering whole app per streamed word | P2 | M | 001 (after 006) | TODO |
| 007 | Cancel stale streaming reads | P3 | M | 001 | TODO |
| 011 | Perf polish (debounce persistence, scope idle tick) | P3 | S | 001 (after 010) | TODO |
| 012 | Minimize bridge audit-log content | P3 | S | 002 | TODO |
| 013 | Fix actively-wrong docs (FEATURES, INTEGRATION_PLAN) | P3 | S | — (after 008) | TODO |
| 014 | Bridge hardening (fail-closed key gate, symlink-safe rooting) | P3 | S | 002 | TODO |
| 015 | Housekeeping (gitignore artifacts, surface dead-model errors) | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Recommended sequence

1. **001 first, always** — it builds the `npm run verify` gate (v2 typecheck +
   lint + `node --test` + v2 build). Plans 002/003 cannot run without the test
   runner; 004/005/006/008/010 all rely on the verify gate.
2. **Security next** (highest-leverage after the gate): 002 (guard tests) → 004
   (command→PI confirm + `/pi` denylist, extends 002's tests) and 005 (client-doc
   leak, extends 002's harness).
3. **Foundation cleanup**: 003 (validator tests), 006 (correctness bundle), 008
   (retire v1), 009 (CLAUDE.md — after 008 so its commands are final).
4. **Perf**: 010 (memoization + line cap; sequence after 006 since both edit
   `v2/App.tsx`), then 011 (polish).
5. **Polish/hardening**: 007, 012, 013, 014, 015 — independent, any order.

## Dependency notes

- 002 → 004, 005, 012, 014: all extend `server/bridge.test.mjs` created in 002 and
  rely on its ephemeral-server harness.
- 001 → everything: the verify gate and v2 typecheck are the baseline.
- 008 → 009, 013: CLAUDE.md and the docs should describe the post-v1-retirement
  reality (default scripts targeting v2, `src/` gone).
- 006 and 010 both edit `v2/App.tsx` — do 006 first (small, surgical), then 010
  (structural), to keep merges clean. 011 after 010.

## Findings considered and rejected (so they aren't re-audited)

- **md.ts → dangerouslySetInnerHTML XSS**: NOT a finding — `esc()` HTML-escapes the
  full string before any markup is built; captured groups land only in element
  text positions; every `style=` attribute is a hardcoded literal, so no
  attribute-injection sink and no link/`javascript:` vector. Confirmed safe for
  LLM/transcript-derived input. (A regression *test* is still valuable → plan 003.)
- **Bridge network posture (DNS-rebinding / CORS / token)**: NOT a finding — loopback
  bind, Host allowlist, OPTIONS→403, bearer token + JSON-only, per-session
  `randomUUID` token held in React state (not localStorage/URL), no-shell `/pi`
  argv, strict sessionId regex, hard-sanitized `/file` slug. Solid.
- **Chrome extension native-host**: NOT a finding — `allowed_origins` locked to the
  single extension id; the host only boots Vite; the CRX signing key is `0600` +
  gitignored.
- **`streamChild` output cap / demo-loop + socket cleanup / buildMd NaN**: NOT
  findings — traced and correct (truncation marker is sent; `running=false` breaks
  stop in-flight loops; the empty-`sentiments` NaN is only referenced inside a
  `length > 0` guard).
- **Denylist-not-allowlist on `/run`, keys served to the browser, transcript sent
  to OpenRouter**: by-design decisions recorded in ROADMAP Phase 3 / "Known
  follow-ups" / ADAPTIVE-NAVIGATOR §5. Implementations match the docs; real fix is
  the roadmap's kernel move (6.3 / 7.4). Not re-reported.
- **`npm audit` clean, socket.io-client current**: no dependency-security findings.

## Deferred (acknowledged, not planned this pass)

- **TypeScript 6 → 7 bump** (DEPS-02): the only major-version lag with real value,
  but low urgency (audit clean, nothing EOL) and MED risk. Sequence it *after* 001
  (so v2 is actually typechecked) and 008 (so the dead v1 tree isn't in the way).
  When ready: bump `typescript` to 7.x, run `npm run verify`, fix new type errors
  surfaced in v2. Treat `@types/node` major lag as a routine minor bump alongside.
- **Direction findings** (kernel convergence 6.2/6.3/7.4, auto-prep at connect,
  finish the Chrome side-panel, kill dead feature toggles): surfaced in the advisor
  report but not turned into plans this pass, per the operator's choice. Revisit
  with `improve next` when ready to scope a design/spike plan.
